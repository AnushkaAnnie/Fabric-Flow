{
  "name": "yarn-fabric-process",
  "version": "1.0.0",
  "description": "Yarn to Fabric tracking system root",
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "npx concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build:frontend": "cd frontend && npm run build",
    "electron:start": "cd electron && npm start",
    "electron:pack": "cd electron && npm run pack",
    "electron:build": "npm run build:frontend && npm run electron:pack",
    "app:offline": "npx concurrently \"cd backend && npm run start\" \"npm run electron:start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
